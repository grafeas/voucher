language: go

cache:
  directories:
    - $GOPATH/pkg/mod

env:
- GO111MODULE=on

go:
  - 1.13.x

script:
  - make ensure-deps
  - make test
  - make test-integrations
  
after_success:
  - bash <(curl -s https://codecov.io/bash)
